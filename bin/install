#!/bin/bash

INSTALL_DIR=$HOME/.local/bin

if [ ! -e $INSTALL_DIR/ghc-mod ] ; then
    echo "ghc-mod was not found"
    exit 2
fi

cp ghc-mod-cache $INSTALL_DIR/
cp ghc-options $INSTALL_DIR/
cp hscopeall $INSTALL_DIR/
cp hxt $INSTALL_DIR/
cp do_at_stack_root $INSTALL_DIR/
